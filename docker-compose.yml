services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: estacionamento-frontend
    ports:
      - "4200:80"
    networks:
      - estac-network
    restart: unless-stopped

networks:
  estac-network:
    external: true
